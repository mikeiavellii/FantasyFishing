import { CurrencyPipe } from "@angular/common";

export interface User {
    id: number;
    userName: string;
    googleName:string;
    currency: number;
    betteRod: boolean;
    cleanWaters: boolean;
    fasterReel: boolean;
    userLevel: number;
}
